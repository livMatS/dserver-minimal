# Python dependencies for the single-container dserver image.
#
# The dserver core and plugins are installed from the main branch of their
# GitHub repositories (latest development state, intentionally not pinned).
dservercore @ git+https://github.com/jic-dtool/dservercore.git
dserver-search-plugin-mongo @ git+https://github.com/jic-dtool/dserver-search-plugin-mongo.git
dserver-retrieve-plugin-mongo @ git+https://github.com/jic-dtool/dserver-retrieve-plugin-mongo.git
dserver-direct-mongo-plugin @ git+https://github.com/livMatS/dserver-direct-mongo-plugin.git
dserver-dependency-graph-plugin @ git+https://github.com/livMatS/dserver-dependency-graph-plugin.git
dserver-notification-plugin @ git+https://github.com/livMatS/dserver-notification-plugin.git

# Flask ecosystem compatibility caps. dservercore leaves Flask/Werkzeug/marshmallow
# unbounded; 2026-era latest Werkzeug 3 + marshmallow 4 break the resolution and
# drag flask-smorest down to an ancient release that imports the long-removed
# flask.views.MethodViewType. These caps keep the stack coherent.
Flask==2.3.3
Werkzeug==2.3.8
marshmallow<4

# dtool CLI for creating/indexing datasets inside the container (PyPI, pinned).
dtool-cli==0.7.1
dtool-info==0.16.2
dtool-create==0.23.4
dtool-s3==0.14.1

# Python lookup API/client, handy for poking the running server (PyPI, pinned).
dtool-lookup-api==0.10.3
dtool-lookup-client==0.1.0

# Runtime: WSGI server + Postgres driver (binary wheel → no gcc needed).
gunicorn==26.0.0
psycopg2-binary==2.9.12
